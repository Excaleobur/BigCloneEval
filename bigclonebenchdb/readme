## BigCloneBench Dataset

The dataset (`bcb.h2.db`) is required for running the BigCloneEval tool. Due to its size, the dataset is not included in this repository 
but can be downloaded from google drive.

[Download BigCloneBench Dataset (bcb.h2.db)]

